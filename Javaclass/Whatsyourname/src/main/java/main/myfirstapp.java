package main;

class MyFirstAppTest {
    public static void main(String[] args) {
        //create an instance of the class
        //String name = "Mikaila";

        MyFirstApp name = new MyFirstApp();
        //We want a program that keeps asking us for
        //until the name entered in = alice or bob
        //dow while or whileloop
        //if else
        // keep count
        int q = 1;
        while (q < 5){
            q++;
            System.out.println();
        }
    }

    private static class MyFirstApp {
    }
}
